info:
  name: Get Task by Status
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/api/tasks/status/TODO"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
