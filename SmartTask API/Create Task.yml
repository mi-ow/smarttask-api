info:
  name: Create Task
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/tasks"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
