info:
  name: Delete Task
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/api/task/id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
