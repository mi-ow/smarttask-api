info:
  name: Update Task
  type: http
  seq: 6

http:
  method: PUT
  url: "{{baseUrl}}/api/tasks/id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
